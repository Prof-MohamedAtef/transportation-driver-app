import 'package:flutter/cupertino.dart';

class VerificationPhaseScreen extends StatelessWidget {
  const VerificationPhaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
